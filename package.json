{
  "name": "blackjak",
  "private": true,
  "version": "0.4.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build && node scripts/build-sw.mjs && node scripts/verify-dist.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "release:check": "npm test && npm run typecheck && npm run build"
  },
  "devDependencies": {
    "typescript": "5.8.3",
    "vite": "7.3.6",
    "vitest": "3.2.7"
  },
  "description": "Mobile-first BlackJak v0.4.0 demo: illustrated table, reactive Jak dealer, themed card decks, startup intro, menu/gameplay music, synthesized SFX, Daily Hand, local progression, and offline PWA support.",
  "engines": {
    "node": ">=22"
  }
}
