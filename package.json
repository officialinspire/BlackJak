{
  "name": "blackjak",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build && node scripts/build-sw.mjs && node scripts/verify-dist.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "release:check": "npm test && npm run typecheck && npm run build"
  },
  "devDependencies": {
    "typescript": "5.8.3",
    "vite": "7.1.7",
    "vitest": "3.2.4"
  },
  "description": "Mobile-first BlackJak visual demo: illustrated table, reactive Jak dealer NPC, three sprite card decks, classic blackjack, Jak's House arcade rules, local progression, Daily Hand, and offline PWA support.",
  "engines": {
    "node": ">=22"
  }
}
